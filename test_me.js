const http = require('http');

const body = JSON.stringify({ email: 'karmansingh@test.com', password: 'password123' }); // I will just create a tiny node script to test login and see user fields, wait, instead of guessing, I can look at frontend code using User type!
